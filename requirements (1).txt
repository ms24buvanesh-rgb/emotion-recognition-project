# ============================================================
# Face Emotion Recognizer — Python Dependencies
# ============================================================
# Install with:
#   pip install -r requirements.txt
#
# Python 3.8 – 3.11 recommended.
# TensorFlow 2.x ships Keras internally; no separate keras install
# is needed unless you are on TF < 2.0.
# ============================================================

# Web framework & CORS support
flask>=2.3.0,<4.0.0
flask-cors>=4.0.0

# Deep learning (includes tf.keras)
tensorflow>=2.12.0,<3.0.0

# Computer vision — Haar Cascade & image processing
opencv-python-headless>=4.7.0   # headless: no GUI dependencies (server-friendly)

# Numerical computing
numpy>=1.23.0,<2.0.0

# Image handling
Pillow>=9.0.0
